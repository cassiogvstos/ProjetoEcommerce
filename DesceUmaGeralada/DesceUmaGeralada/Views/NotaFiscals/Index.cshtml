@model IEnumerable<DesceUmaGeralada.Models.NotaFiscal>

@{
    ViewData["Title"] = "NotaFiscal";
}

<h2>Nota Fiscal</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NotaFiscalID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Tipo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Observacao)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NotaFiscalID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Observacao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NotaFiscalID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NotaFiscalID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NotaFiscalID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
